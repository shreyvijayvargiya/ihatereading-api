# Dashboard uses the parent API repo Firebase admin credentials
# (config/service-account-file.js + optional repo-root .env).
# Table reads do not need `npm run start`.
#
# Agent start/stop still proxies to the API when it is running:
# VITE unused — Vite plugin loads ../.env
